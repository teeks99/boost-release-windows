"""Distributed Boost-for-Windows release builds.

Every command is driven by ``build.toml`` and works the same way on a laptop,
on a big build VM and inside a GitHub Actions job.  See ``README.md``.
"""

__version__ = "2.0.0"
